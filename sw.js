const CACHE='sidequest-v24';
const ASSETS=['./','./index.html','./styles.css','./friends.css','./quick-progress.css','./app.js','./config.js','./sync.js','./manifest.webmanifest','./icon.svg'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(ASSETS)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;if(event.request.mode==='navigate'){event.respondWith(fetch(event.request).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put('./index.html',copy));return response}).catch(()=>caches.match('./index.html')));return}event.respondWith(caches.match(event.request).then(cached=>cached||fetch(event.request).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(event.request,copy));return response}))) });
self.addEventListener('message',event=>{if(event.data?.type!=='TEST_NOTIFICATION')return;event.waitUntil(self.registration.showNotification('Sidequest is armed',{body:'Notifications work on this device. No academic disasters detected.',icon:'./icon.svg',badge:'./icon.svg',tag:'sidequest-test',data:{url:'./'}}))});
self.addEventListener('push',event=>{let message={};try{message=event.data?.json()??{}}catch{message={body:event.data?.text()}}event.waitUntil(self.registration.showNotification(message.title??'Sidequest',{body:message.body??'You have a new Sidequest update.',icon:'./icon.svg',badge:'./icon.svg',tag:message.tag??'sidequest-alert',data:{url:message.url??'./'}}))});
self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(windows=>{for(const window of windows){if('focus'in window)return window.focus()}return clients.openWindow(event.notification.data?.url??'./')}))});
